#version 420 core

layout (location = 0) in vec3 pos;

layout (std140) uniform matrices {
	mat4 model;
	mat4 view;
	mat4 proj;
};

out vec3 ws;

void main(){
	ws = vec3(model*vec4(pos, 1.0));
	gl_Position = proj*view*vec4(ws, 1.0);
}